<%= title l(:label_bill_new) %>

<%= form_for @bill do |f| %>
  <%= error_messages_for 'bill' %>
  <div class="box tabular">
    <div id="all_attributes">
    <%= render :partial => 'bills/form', :locals => {:f => f} %>
    </div>
  </div>

  <%= submit_tag l(:button_create) %>
<% end %>
